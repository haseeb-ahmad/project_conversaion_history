<!DOCTYPE html>
<html lang="en">
<head>
  <title><%= content_for(:title) || "Project Conversations History" %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="mobile-web-app-capable" content="yes">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.14.8/dist/cdn.min.js"></script>
  <%= yield :head %>

  <!-- Favicon and PWA icons -->
  <link rel="icon" href="/icon.png" type="image/png">
  <link rel="icon" href="/icon.svg" type="image/svg+xml">
  <link rel="apple-touch-icon" href="/icon.png">

  <%# Styles & JS ImportMap %>
  <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body class="bg-gray-100 text-gray-900 font-sans antialiased">
  
  <!-- Header -->
  <header class="bg-white shadow-md fixed top-0 w-full z-10">
    <div class="container mx-auto flex justify-between items-center px-6 py-4">
      <!-- Logo -->
      <h1 class="text-xl font-bold text-gray-800">
        <%= link_to "Project Conversations", root_path, class: "hover:text-blue-600 transition" %>
      </h1>

      <!-- Navigation -->
      <nav class="flex items-center space-x-4">
        <%= link_to "Home", root_path, class: "text-gray-700 hover:text-blue-600 px-4 py-2 transition" %>

        <% if user_signed_in? %>
          <!-- Logout Button -->
          <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "bg-red-500 text-white px-4 py-2 rounded-lg hover:bg-red-600 transition" %>
        <% else %>
          <!-- Login Button -->
          <%= link_to "Login", new_user_session_path, class: "bg-blue-500 text-white px-4 py-2 rounded-lg hover:bg-blue-600 transition" %>
        <% end %>
      </nav>
    </div>
  </header>

  <!-- Main Content -->
  <main class="container mx-auto mt-28 px-5 py-6 bg-white shadow-lg rounded-xl">
    <%= yield %>
  </main>

  <!-- Footer -->
  <footer class="bg-gray-800 text-white text-center py-4 mt-10">
    <p class="text-sm">&copy; <%= Time.current.year %> Project Conversations. All rights reserved.</p>
  </footer>

</body>
</html>
